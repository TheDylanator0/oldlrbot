const discord = require("discord.js")

module.exports = {
    name: 'autocorrect',
    description: "staff team",
    execute(message, args){
            message.channel.send("Autocorrect is terrible! `turn it off right now`")


    }
}